{
  "name": "mock-project-tests",
  "version": "1.0.0",
  "description": "E2E Playwright test suite for tag pagination and follow features",
  "main": "index.js",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
